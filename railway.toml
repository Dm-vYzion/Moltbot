[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# We'll stick to the defaults the bot prefers
startCommand = "moltbot gateway --port 18789 --host 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
