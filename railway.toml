[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# no startCommand – Railway will use the container CMD
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
