[build]
builder = "NIXPACKS"

[deploy]
# We use 'npx' which is the standard way to run an npm binary without a path
startCommand = "npx moltbot gateway --port 18789 --host 0.0.0.0"
healthcheckProtocol = "TCP"
healthcheckTimeout = 300